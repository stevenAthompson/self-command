{
  "name": "self-command",
  "version": "1.0.1",
  "mcpServers": {
    "selfCommand": {
      "command": "node",
      "args": ["${extensionPath}${/}dist${/}self_command.js"],
      "cwd": "${extensionPath}"
    }
  }
}